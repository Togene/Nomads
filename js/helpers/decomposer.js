function decomposer(ss, frames, colors, offset, mat, type){
    this.ssIndex = ss;
    this.animationFrames = frames;
    this.colors = colors;
    this.centre_offset = offset;
    this.matrix = mat;
    this.type = type;
}